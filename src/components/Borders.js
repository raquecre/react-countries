

const Borders = ({ countrySelectedBorders }) => {
    console.log({countrySelectedBorders});

    /* return (
        { country.borders.map((border) => <p>{border}</p>) }
    ) */
}

export default Borders;